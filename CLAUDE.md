# Falcon BMS Companion: notes for AI-assisted maintenance

Three deliverables live in this repo, all built from the same Kotlin/Compose screens in `app/src/main/java`:
1. **Android app** `app/`: Kotlin + Jetpack Compose + Material 3, package `com.bmscompanion.app`, a single APK (charts and every map style/zoom level included).
2. **BMS Companion for Windows** `desktop/`: Compose for Desktop. It reads Falcon BMS (the former C# bridge, ported to Kotlin in `desktop/.../bridge/`), serves the API, the browser version and the bundled data on one port (47474), and is the full app. One window: the light **server page** or the **full app**. Packaged as MSI + portable zip with its own Java runtime.
3. **Browser version** `web/`: Kotlin/Wasm + Compose for Web. The same app running in the browser (iPhone, iPad, any browser); served by the PC program from its resources (`/webapp`).

## Ground rules
- **The BMS install is read-only.** Never write, move or delete anything in the BMS folder, including during testing. To test EZBoards, copy its folder to a temp dir and strip the `SET KNEEBOARD[...]` lines from the copy's `CONFIG_USER.BAT`.
- **No personal data in the repo** (Windows user names, callsigns, absolute user paths, LAN addresses in screenshots). `local.properties`, `dist/`, `posts/` and `tools/pdftext/` are git-ignored. The demo briefing in `desktop/src/main/resources/bridge` is sanitized. Screenshots come from demo mode (env `BMSC_DEMO=1`).
- **Phone layouts must stay usable.** Tablet-specific layouts use `isWide()` (≥840dp) / `isMedium()` (≥600dp), `AdaptiveSplit` and `Masonry` from `ui/components/Adaptive.kt`. The PC window and the browser page feed their width into the same checks.
- **Shared app code must also compile for the PC and the browser.** After changing `app/src/main/java`, run `./gradlew :desktop:compileKotlin :web:compileKotlinWasmJs`. Don't add Android-only APIs to shared screens (stand-ins: `desktop/src/main/kotlin/shims/`, `web/src/wasmJsMain/kotlin/shims/`). Prefer `kotlin.*` over JVM APIs; the browser version only has stand-ins for `String.format` (patterns in `web/.../Printf.kt`), `Math`, `System`, `Locale` casing, `SimpleDateFormat`/`Date`, `synchronized` (per package in `web/.../shims/jvmapis/`).
- **JSON contract changes are additive.** The server serializes the app's `app/.../data/mission/MissionModels.kt` classes directly; update the producing code in `desktop/.../bridge/` and `docs/PROTOCOL.md`.

## Where things are
| Topic | Files |
|---|---|
| **Version and the project's own details** (one source: Gradle reads NAME/BMS out of this file for the APK's versionName and the installer) | `app/.../AppVersion.kt`; About page `app/.../ui/screens/About.kt` (link opener `Platform.openUrl` in `data/ImageActions.kt`, set by each platform's entry point) |
| Installer artwork and **upgrading over an installed copy** (both done to the finished MSI: jpackage offers no way in, and its product code is derived from vendor/name/version alone, so every build of one version collides) | `pc/make-installer-art.mjs` → `desktop/installer/*.bmp`, `pc/finish-msi.ps1`, `finishMsi` in `desktop/build.gradle.kts` |
| Navigation, tabs, routes (`m/...` = pages opened from Mission, owned by the Mission tab); `AppRoot(startRoute, nav)` | `app/.../ui/AppRoot.kt` |
| Bundled data loading, prefs | `app/.../data/Repo.kt`, `data/Models.kt` (PC: `desktop/.../overrides/data/Repo.kt`; browser: `web/.../overrides/data/Repo.kt`, data over HTTP, prefs in localStorage) |
| Theater map widget (projection x=north ft, y=east ft) | `app/.../ui/components/TheaterMap.kt` (PC and browser: `desktop/.../overrides/ui/components/TheaterMap.kt`) |
| Map styles, tile levels, landmarks (borders, provinces, labels, towns), the **Map** menu (`MapLook` prefs), mission towns (`MapFocus`/`MapMission`, set by `PublishMapMission` in `MissionTabs.kt`; `GeoPaths.relevant`) | `app/.../ui/components/MapBase.kt`; data `assets/maps/<mapId>/<style>.webp` + `<style>/<z>/<r>_<c>.webp`, `assets/data/geo/<mapId>.json`; made by `tools/extractor/src/maps.mjs`, `geo.mjs`, `projection.mjs` |
| Shared list and search furniture: `SearchField` (grows an on-screen keyboard when `Hud.onPaper`), `ChipRow` (draws nothing on a kneeboard), `AlphabetScrubber` (A-Z strip for an alphabetical `LazyColumn`; pass how many rows precede the data) | `app/.../ui/components/Components.kt`; used by `Airports.kt`, `Browse.kt` |
| Skins: the app's dark HUD, and paper day/night for a kneeboard (`Hud.*` are dynamic getters over `HudSkin`, so all ~1,100 uses re-colour at once; never copy one into a top-level `val` — nor into a `remember`ed style, which freezes it at first composition) | `app/.../ui/theme/Theme.kt`; `Hud.onPaper` switches layout, e.g. `SectionCard` drops its panel. A board's **map** is drawn in screen inks: the drawing pass runs inside `inMapInks { }` (a plain, non-state `HudSkin.mapPass` flag for one draw), and symbol colours test `Hud.paperInks`, not `onPaper` — paper inks sank into the relief in the headset |
| The board as an object (spiral head, rim, aged sheet) and the page inset inside it | `app/.../ui/Kneeboard.kt` (`BoardFurniture`, `PaperSheet`, `HEAD`/`RIM`) |
| Board Dashboard (map takes the space the cards do not; tall boards split top/bottom, others left/right) | `app/.../ui/screens/mission/MissionDashboard.kt` (`BoardDashboard`) |
| The map covers its box on a board instead of fitting inside it (`cover`/`minScale`, both TheaterMap copies) | `app/.../ui/components/TheaterMap.kt`, `desktop/.../overrides/ui/components/TheaterMap.kt` |
| VR boards (`/kneeboard/<n>`, one OpenKneeboard tab each, no controls): kinds, page building and the page counter | `app/.../ui/board/VrBoard.kt`; the configuration page (PC only) `desktop/.../overrides/ui/screens/mission/MissionVrBoards.kt`, wired through `vrBoardsPane` in `MissionTabs.kt`; stored in `bridge-settings.json` as `Boards`, served at `/api/boards` |
| VR board print size: a board is laid out at a few hundred points wide whatever its pixel size (`Kneeboard.textSizes`, `densityFor`, applied to `LocalDensity` in `AppRoot`); ☰ → Print size | `app/.../ui/Kneeboard.kt`, `app/.../ui/AppRoot.kt` |
| VR board pages (OpenKneeboard page-based content, experimental `PageBasedContent@2024073001`): each section is a page, so a bound button flips them with no pointer — OpenKneeboard has no mouse in-game. Published from `AppRoot`, event polled through a JS global | `web/.../Browser.kt` (`okbPublishPages`, `okbTakePage`), `app/.../ui/Kneeboard.kt` (`publishPages`, `takePage`) |
| Planned tanker/AWACS tracks: `MissionArchive` opens the `.cam`/`.tac` (directory at the end, per-part LZSS), `PlannedRoutes` finds flight records by signature (type id repeated 10 bytes on, class table says flight), reads their waypoints, swaps the campaign's east-first grid into our north-first feet, and only returns them when the file also holds the DTC's own steerpoints; `Bridge.supportTracks` marks the longest-hold leg as the station. A campaign waypoint is a grid **cell**: the position is its middle, so half a cell (1640 ft) is added on both axes — without it every track sits 0.27 nm south-west. The **class table** (which unit types are flights) is found through the theater definition's `objectdir` first (`classTable`/`objectDirOf`): a campaign pack such as Hellas WCP has no objects of its own and points at its parent theater, and reading Korea's table instead left every add-on aircraft (types 6500+, your own flight and the AWACS among them) invisible, so nothing was believed and only the briefing's station circle was drawn. Your flight window ignores untimed points (routes end with a 00:00 alternate landing). Developer checks `--atotest`, `--axistest`, `--trackstest` (also prints the gap to your own steerpoints), `--supporttest <save> <your flight callsign> out.txt` (every tanker/AWACS through the same tests as `supportTracks`, with the reason kept or dropped), `--anytype <save> out.txt` (routes at types the class table does not call flights — the symptom of a wrong class table), `--basetest` (every route's take-off and landing point against the known airfields: the mean offset is the reading error, and is how the half cell was found) | `desktop/.../bridge/MissionArchive.kt`, `PlannedRoutes.kt`, `Bridge.kt` |
| First-run prerequisites (.NET 8, WebView2, OpenKneeboard) with links to the official pages; shown once per version, then only while something is missing | `desktop/.../Prerequisites.kt`, `PrerequisitesCard` in `ui/PcCards.kt` |
| Friend, foe, neutral and your flight: the Tacview `Coalition` is the **country** ("Hellas", "U.S."), not the side, so `TacviewClient.snapshot` asks `TeamRelations` (the campaign's `.tea` part: per team, a stance toward each of the 8 teams at +22, name at +453; found by signature, all-or-nothing) through `TacviewClient.relation`. Allied/friendly → `friendly`, neutral/none → `neutral`; `wingman` = same callsign but the last digit. The app counts threats with `Contact.hostile` (not `!friendly`). All twelve saves on the development machine, nine theaters, read: the neutral slot may be allied with itself or have no relations at all, a team may be controlled by another (Korea: ROK controls the U.S. team, so allied units arrive as "ROK" — Tacview writes the **team** name), a team may be neutral toward itself (LHTO), and records differ in size (LHTO), so they are anchored on the team entity type learned from the first record. Checks: `--teamtest` (the table), `--teamfile out.txt <save>…` (any saves), `--sidetest <recording.txt> <callsign> <seconds> out.txt` (a recording replayed through the client) | `desktop/.../bridge/TeamRelations.kt`, `TacviewClient.kt`, `app/.../data/mission/MissionModels.kt` (`Contact`), `MissionMap.kt` (`contactColor`, `Wingman`) |
| Air defences on the map: the Tacview client forwards ground air-defence objects as kind `sam`, one per site (`sites()` clusters a battery, `MAX_SITES` caps it); the app matches the name against the threat reference for the ring's radius (`samSites`, `SamSite`), drawn under `MapLayers.sams` with the pre-planned PPT rings | `desktop/.../bridge/TacviewClient.kt`, `app/.../ui/screens/mission/MissionUtil.kt`, `MissionMap.kt` |
| Support stations: BMS publishes no route for a tanker or an AWACS, so the briefing's own sentence ("orbiting 20 nm northeast of Larissa") is parsed (`stationFromNotes`, `SupportStation`) and resolved against the theater's airfields and `data/geo` towns; drawn under `MapLayers.support` | `app/.../ui/screens/mission/MissionUtil.kt`, `MissionMap.kt` |
| A board's three voices: `SheetTitle` (accent ink, rule), `ColumnScopeLike.Head` (accent bar), `Para`/`Note`; `Chips` boxes the figures a pilot reaches for (TACAN, UHF, bullseye) and `Title` names a block. The page names itself for OpenKneeboard's tab list through `Kneeboard.setTitle` (web `setDocumentTitle`) | `app/.../ui/board/VrBoard.kt`, `app/.../ui/Kneeboard.kt`, `web/.../Browser.kt` |
| A board's chrome: no footer strip — the page counter is a faint overlay in the corner (`VrBoardScreen`), the bubble says what the turn did, and the sheet keeps `SHEET_HEAD`/`SHEET_FOOT`/`SHEET_SIDE` from the frame. Ruled tables: `ColumnScopeLike.Table` (spans a short row across the remaining columns), `Facts` (two facts a line, long ones full width) and `Cols` (hairline rules, first column wraps) | `app/.../ui/board/VrBoard.kt` |
| A board's page is titled by the **server** (`PcServer.sendPage`/`boardTitle` → "BMS KB 2 (Live map)"), because OpenKneeboard names a new tab from the title of the page it loads — long before the app could set it; `Kneeboard.setTitle` keeps it right afterwards | `desktop/.../PcServer.kt`, `app/.../ui/board/VrBoard.kt` |
| One sweep per version of what older ones left in `%APPDATA%\BMS Companion` (cached installer, stale lock/port, old logs, the pre-1.3 bridge's folders). Runs after `SingleInstance.acquire`, so the lock it removes is certainly stale; never touches `bridge-settings.json` or `pc-app.properties` | `desktop/.../Housekeeping.kt` |
| The port is never moved: `PcServer.start` keeps retrying the configured port for a minute (an update's old copy may still hold it) rather than failing or picking a free one — every address a pilot pasted into OpenKneeboard depends on it | `desktop/.../PcServer.kt` |
| Board pages of print are **measured, not estimated**: `packedPages`/`PackedSheet` subcompose every block, break where the sheet runs out, and report the cut points back as state (that count is what is published to OpenKneeboard). `maxSheets` sets the print down a step at a time to keep a board (the briefing) to two sheets. Margins: `SHEET_SIDE`/`SHEET_HEAD`/`SHEET_FOOT`, `PLATE_MARGIN` | `app/.../ui/board/VrBoard.kt` |
| The page **ring** (OpenKneeboard stops at the last page it was given, so the tab gets a run of `sheets × 3` pages and page `i` shows sheet `(base + i) mod sheets`; a fresh run is laid down at the end of the old one), the map board's pages as **zoom steps** (`MAP_ZOOMS`, `MAP_ZOOM_LABELS`) and the fading **page bubble** | `web/.../Browser.kt` (`okbPublishPages`, `__bmscBase`/`__bmscRun`), `app/.../ui/board/VrBoard.kt` (`PageBubble`, `BoardMap(step)`) |
| Exported kneeboard (UOAF html_brief): finding the tool, reading its `config.ini`, rendering its pages (PDFBox), staleness against the printed briefing | `desktop/.../bridge/ExportedKneeboard.kt` (setting `KneeboardExporterDir`, `/api/kneeboard/page`); Briefing card and the run button `app/.../ui/screens/mission/MissionKneeboard.kt`; chart-viewer source `app/.../ui/screens/ChartSource.kt` (`KNEEBOARD_SCHEME`); board kind `EXPORTED` in `VrBoard.kt` |
| ACMI recordings (folder size, clear to the Recycle Bin, never `.vhs`) | `desktop/.../bridge/AcmiStore.kt`, `AcmiRow` in `desktop/.../ui/PcCards.kt` |
| VR board shape (OpenKneeboard reshapes a Web Dashboard tab to the pixel size the page asks for; `SetPreferredPixelSize`): asked once in `index.html` before the app starts, then by the app from the remembered choice; ☰ → Board shape | `web/.../resources/index.html`, `web/.../Browser.kt` (`askBoardPixelSize`), `app/.../ui/Kneeboard.kt` (`shapes`, `askShape`, `chooseShape`) |
| Kneeboard layout (**browser only**, address `/kneeboard`, also `?kneeboard=1`/`#kneeboard`; served by `PcServer.kt`, read in web `Main.kt`. For OpenKneeboard in VR; the Android app and the PC window never enter it) | `app/.../ui/Kneeboard.kt` (`KneeboardFrame` = auto-hiding ☰ + ⋯ chrome, `KneeboardMenuSlot`/`KneeboardActionsSlot` for what a page adds), `AppRoot.kt`, web `MissionScreen.kt`, `kneeboardMissionTabs` in `MissionTabs.kt`, ⋯ menu in `MissionMap.kt` |
| Tankers & support (TACAN, UHF, location) | `app/.../ui/screens/mission/MissionSupport.kt` (Dashboard card `SUPPORT`, Briefing, Comms) |
| Mission client (polling while screen shown, UDP discovery) | `app/.../data/mission/MissionLink.kt`; PC `desktop/.../overrides/data/mission/MissionLink.kt` (`LinkMode.LOCAL` calls `Bridge.handle` in-process); browser `web/.../overrides/data/mission/MissionLink.kt` (same-origin `/api`) |
| Mission UI | `app/.../ui/screens/mission/*.kt`: tabs and shared plumbing in `MissionTabs.kt` (Dashboard / Map / AWACS / Briefing / Comms / Kneeboards / Setup); `MissionDashboard.kt` (cards, span masonry, saved layouts), `MissionAwacs.kt` + `AwacsTools.kt` (GCI geometry and calls) |
| Mission tabs after the 1.3.4 tidy-up: **no Flight tab** (its four panels were already Dashboard cards; `MissionFlight.kt` keeps `FlightTiles`/`RwrScope`/`DedPanel` for them, and the "In flight" preset restores its layout), **AWACS hidden** unless `MissionTabPrefs.showAwacs` (`TabsCard` on both Setup panes; off by default everywhere), **Kneeboards** = the old Boards + VR boards + the Briefing page's HTML Briefing card. `vrBoardsPane` is a section of that page, so `MissionVrBoardsPane` must not scroll itself | `app/.../ui/screens/mission/MissionTabs.kt`, `MissionBoards.kt`, `MissionKneeboard.kt` (`KneeboardPages`), `desktop/.../overrides/ui/screens/mission/MissionVrBoards.kt` |
| A fold that is remembered between launches: `CollapsibleCard(rememberKey = …)` keeps it in the prefs file (`open_<key>`), which an update never rewrites | `app/.../ui/components/Components.kt` |
| Media (BMS screenshots) | `app/.../ui/screens/Media.kt` (grid, viewer), platform share/download actions via `data/ImageActions.kt` (`Platform.imageActions`, `LocalImageActions`; PC `desktop/.../PcImageActions.kt`, browser `web/.../Main.kt`); server side `desktop/.../bridge/Screenshots.kt` (thumbnails, Recycle-Bin delete) |
| Reading BMS: shared memory struct offsets and reader | `desktop/.../bridge/SharedMemory.kt` (mirrors `Tools/SharedMem/FlightData.h`; JNA) |
| Briefing / DTC parsers | `desktop/.../bridge/BmsFiles.kt` |
| AWACS picture | `desktop/.../bridge/TacviewClient.kt` (Tacview real-time telemetry, port 42674) |
| Updating in the app (About): release list, notes, download with rate and time left, SHA-256 check, cache | `app/.../data/update/Updates.kt`; PC `desktop/.../PcInstaller.kt`, Android `app/.../AndroidInstaller.kt` |
| EZBoards | `desktop/.../bridge/EzBoards.kt` (hidden `cmd /c EZBOARDS.BAT companion`; board tables via `bin\xbrief.exe`) |
| API routes, file watching, demo mode, settings (`bridge-settings.json`) | `desktop/.../bridge/Bridge.kt`, `DemoSource.kt`, `BridgeSettings.kt` |
| HTTP server (API, forwarding for clients, browser version, `/assets`), discovery | `desktop/.../PcServer.kt`, `desktop/.../bridge/Discovery.kt` |
| Folder pickers and the error log (`%APPDATA%\BMS Companion\error.log`, `PcLog.install()` in `Main.kt`) | `desktop/.../ui/PcCards.kt` (`pickFolder`), `desktop/.../PcLog.kt` |
| Window (server page / full app, F11 borderless full screen), tray, single instance | `desktop/.../Main.kt`, `SingleInstance.kt`, `DarkTitleBar.kt` |
| Mode (server page / app), where BMS runs, browser access, services | `desktop/.../PcConfig.kt` (`PcConfig`, `PcServices`) |
| Server page and settings cards (status, connect devices + QR, setup checklist, BMS settings, activity) | `desktop/.../ui/PcScreens.kt`, `ui/PcCards.kt` |
| PC replacements of Android-only files | `desktop/src/main/kotlin/overrides/` (Repo, MissionLink, TheaterMap + wheel zoom, Charts + wheel zoom, MissionScreen with pin/full screen/server buttons, MissionSetup); excluded from the app sources in `desktop/build.gradle.kts` |
| Browser version entry, JS bridges (fetch, storage, download, share), page | `web/.../com/bmscompanion/web/Main.kt`, `Browser.kt`, `web/src/wasmJsMain/resources/index.html` (WasmGC check, splash) |
| Developer checks (`--selftest`, `--dumpstrings`, `--eztest`, `--api`, `--maprender`, `--updatetest`, `--atotest`, `--axistest`, `--trackstest`, `--basetest`, `--teamtest`, `--sidetest`) | `desktop/.../bridge/SelfTest.kt`, `MapRender.kt` |
| Chart viewer gestures: swipe turns the page only while fitted (`onSwipe` on `ZoomableBitmap`, accumulator reset on each pointer-down), pinch/drag pan once zoomed | `app/.../ui/screens/Charts.kt`, `desktop/.../overrides/ui/screens/Charts.kt` |
| Airport charts: BMS plates + instrument chart PDFs (PDFBox → WebP pages, `ChartRef.pages`) | `tools/extractor/src/charts.mjs` (needs `cache/pdfbox-app.jar`); viewer `app/.../ui/screens/Charts.kt` |
| Turning sideways chart pages upright (AIP prints a landscape table on an upright page): typeset pages from the glyph angles in the middle of the page, drawn pages from the long rules in the image (`pageSideways`) | `tools/extractor/src/charts.mjs`, `tools/extractor/src/PageDirs.java` |
| Reference data extraction | `tools/extractor/src/*.mjs` (env `BMS_ROOT`) |
| Manual-derived data | `tools/curated/*.json` |

## Common tasks
- **New BMS version:** follow `docs/UPDATING.md` step by step.
- **Build & run on a device:** `./gradlew installDebug`. Debug builds accept `adb shell am start -n com.bmscompanion.app/.MainActivity --es route mission` to open a route directly (useful for screenshots).
- **PC version from source:** `./gradlew :desktop:run` (builds the browser version into its resources first; the first web build downloads Node/Yarn/Binaryen and takes minutes). Env: `BMSC_ROUTE=mission` opens a route, `BMSC_DEMO=1` demo mission for this run, `BMSC_PORT=47490` another port (e.g. when another copy uses 47474), `BMSC_SHOW_ADDRESS=192.168.1.20` + `BMSC_SHOW_PORT=47474` show an example address on the server page (screenshots without a real LAN address). Settings live in `%APPDATA%\BMS Companion\` (`pc-app.properties`, `bridge-settings.json`, `app.lock`).
- **Browser version:** served at `http://<pc>:47474/` by the running PC program (`?route=mission` opens a route). Headless check: `msedge --headless=new --window-size=1180,820 --virtual-time-budget=40000 --screenshot=out.png http://127.0.0.1:47474/?route=mission` (headless windows are at least 500 px wide).
- **Developer checks:** `"BMS Companion.exe" --selftest out.txt` (struct sizes, parser output), `--dumpstrings out.txt` (with BMS running), `--eztest <EZBoards copy> out.txt`, `--api /api/info,/api/mission out.txt`, `--maprender <theater id> <folder> [xFt,yFt]` (all map styles at three zooms as PNGs, headless). From source: `./gradlew :desktop:run --args="--selftest C:/temp/out.txt"` (call gradlew from PowerShell when the path has spaces).
- **Bump the version:** `NAME` in `app/src/main/java/com/bmscompanion/app/AppVersion.kt` (and `versionCode` in `app/build.gradle.kts`). Nothing else: both build files read that file.
- **Release:** `./gradlew assembleRelease` (copy to `dist/BMS-Companion.apk`) and `powershell -ExecutionPolicy Bypass -File pc/publish-desktop.ps1` (MSI and zip into `dist/`). Release files: APK, MSI, zip. Notes from `CHANGELOG.md`.

## Gotchas
- **The install context has to agree in two places.** `perUserInstall` in `desktop/build.gradle.kts` decides what jpackage builds; `ALLUSERS` in `pc/finish-msi.ps1` decides what the installer asks for. A per-user package sets bit 8 of the summary Word Count ("installs without elevation"), and Windows then refuses `ALLUSERS=1` with "You do not have sufficient privileges to complete this installation for all users of the machine" — elevating does not help, and with UAC off it is never seen. BMS Companion is per machine in both: double-clicking raises the consent prompt on its own. `finish-msi.ps1` fails the build if the two disagree.
- **MSI Upgrade attributes: 0x100 is VersionMin**Inclusive, **0x200 is VersionMax**Inclusive. jpackage writes 257 (MigrateFeatures + 0x100) with an empty VersionMin, so VersionMax is exclusive and a same-version build installs *alongside* the old one instead of replacing it. `pc/finish-msi.ps1` sets 0x200 and fails the build if it is missing. Attributes is part of that table's primary key, so the row has to be deleted and re-inserted, not updated.
- **Swing file choosers must stay off the Windows shell.** The app sets the system look and feel, under which `JFileChooser` walks `sun.awt.shell.Win32ShellFolder2` for icons and the "This PC" list; on many machines (OneDrive, mapped or removable drives) that throws or takes the process down, and it lands on the thread Compose draws on. `pickFolder` sets `putClientProperty("FileChooser.useShellFolder", false)`, opens in a folder that is already known, and catches everything. Every folder it picks can also be typed into Settings.
- **Navigation: one navigate per tap, and no transitions between sections.** `popBackStack` + `clearBackStack` in the same handler, or a cross-fade that keeps a popped destination composing, both end in `IllegalStateException: You cannot access the NavBackStackEntry's ViewModels…` from `LocalOwnersProvider` (navigation-compose 2.8.0-alpha10 asks every composed entry for its ViewModel store). `AppRoot.onTab` uses a single `navigate` for Home and the NavHost uses `EnterTransition.None`/`ExitTransition.None`. Reproduce with rapid rail clicks in the browser build; page errors show up in the CDP console.
- **Java 21 collection methods are a trap.** The app compiles against API 35, so Kotlin binds `LinkedHashSet.reversed()`, `List.getFirst()`, `MutableList.removeFirst()` … as members; they do not exist before Android 15 and throw `NoSuchMethodError` (1.3.1 crashed on Android 9). Write `set.toList().asReversed()`, `list[0]`, `removeAt(0)` instead. Lint does not catch it; `tools/check-apk-compat.ps1` scans the built APK.
- `String.format("%d", double)` crashes at runtime. Convert with `.toInt()` first.
- `TheaterMap` reads `onTap`/`onUserGesture` via `rememberUpdatedState`. Don't key `pointerInput` on lambdas: live data recomposes 4×/s and would cancel taps.
- `briefing.txt` uses CRLF, and EZBoards' `xbrief.exe` fails on LF-only files.
- BMS RWR `bearing[]` is true bearing in radians. Subtract ownship yaw for the scope.
- StringData ids are verified with `--dumpstrings out.txt` against a running BMS (the header comments skip entries). NavPoint `z` is feet (not tens of feet). The hsiBits `Flying` flag is not set in 4.38.1, so flying detection also uses `pilotsStatus == 3`.
- TACAN has two sources in shared memory: UFC (DED, usually the home base from the DTC) and AUX COMM. `live.tacan` prefers the A/A or Y-band one; both are sent as `tacanUfc`/`tacanAux`.
- Tacview sends ejected crews as `Ground+Air+Light+Human+Parachutist`; they map to kind `crew` before the `Air` check.
- **Tacview's `Coalition` from BMS is the country, and `Color` is per country too** (Hellas Blue, Albania Cyan, U.S. Green, N.Macedonia Yellow in one recording). Neither says who is allied; only the campaign's team table does. Comparing coalition names drew every allied aircraft of another nation as hostile.
- DTC `target_N` is steerpoint N+1. Action `-1` marks a user target steerpoint.
- Some launch environments set `NoDefaultCurrentDirectoryInExePath`, and the EZBoards runner removes it for the child cmd.
- `Falcon BMS.exe` detection uses a Toolhelp snapshot (works when BMS runs elevated); `ProcessHandle` would not see its name.
- PC version: don't use `LifecycleResumeEffect` for polling in PC overrides. The window provides its own always-RESUMED `LifecycleOwner`/`ViewModelStoreOwner` (`Main.kt → WindowOwner`), shared with the `NavHostController` hoisted outside the window, because full screen recreates the window (`key(fullscreen)` with `undecorated`).
- PC version: full screen is a recreated undecorated window sized to the monitor, not `WindowPlacement.Fullscreen` (Java's exclusive full screen minimizes when BMS takes focus).
- Browser version: Kotlin/Wasm `LinkedHashMap` is final (no `removeEldestEntry`); `java.*` stand-ins live in packages `java.util`/`java.text` under `web/.../shims`. JS interop goes through `@JsFun`; converting large downloads byte by byte is fine for thumbnails and charts, avoid it for huge files.
- **`PageBasedContent` can only be enabled once per page.** A second `EnableExperimentalFeatures` call answers "already enabled" — an error — and the board publishing again (a count that grew, a kind changed on the PC) then lost its pages. `okbPublishPages` keeps the enabling promise on `window.__bmscFeature` and treats "already enabled" as success; `VrBoard` retries a failed publish a few times.
- **The browser bundle can go stale after a source change.** `compileKotlinWasmJs` and `compileProductionExecutableKotlinWasmJs` re-run, but `…Optimize`, `wasmJsProductionExecutableCompileSync`, `wasmJsBrowserProductionWebpack` and `wasmJsBrowserDistribution` can all stay UP-TO-DATE, so the PC program goes on serving the previous bundle (and `:desktop:run` copies that one into its resources). Symptom: an edit that compiles cleanly has no effect in the browser. Check with `grep <something new> web/build/dist/wasmJs/productionExecutable/bmsc.js`; force it with `rm -rf web/build/compileSync web/build/dist web/build/kotlin-webpack` before the build.
- Browser version needs WasmGC (Safari 18.2+, Chrome/Edge 119+, Firefox 120+); `index.html` shows a message on older browsers.
- Maps: every style, tile level and landmark layer is drawn in the same unit square of the theater (`MapProjection`), so styles always align. The BMS projection is PROJ tmerc from `NewTerrain/Theater.txt` (theater feet = metres × 3.27998; x = north). `HeightMap.raw` is int16 feet. Water is **not** "height ≤ 0": land below sea level (Jordan valley, Arava, Nile delta) has varying heights, while BMS stores each water body as an exactly flat level (Israel: sea -70, Dead Sea -1457, Galilee -771). Use `tools/extractor/src/heightmap.mjs` (`readHeightmap` → `water` mask). Keep `MAX_Z` in `MapBase.kt` and `maps.mjs` equal.
- `data/geo` coordinates must be integers (the app reads them as `List<Int>`; a float fails the whole file).
- Tanker TACAN: BMS assigns 92Y to the first tanker, then 126Y, 125Y… (TO 1F-16CMAM-34-1-1, TACAN). A TACAN in the briefing notes wins. Frequencies follow the callsign in the theater's RadioMap (`data/radio`).
- BMS does not export the text of its AWACS/ATC radio calls: the sim assembles them from `Data/Sounds/CommFile.xml` fragments and draws subtitles itself. Shared memory, the Tacview stream (`0,Event=Message` carries weapon events only) and the log files have nothing, so the mission log derives events instead of transcribing calls.
- Never test Media delete on the real BMS folder: point `PicturesDirOverride` at a copy (Falcon BMS settings), and clear it afterwards.
